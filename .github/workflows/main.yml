on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    name: Updates Blog Outline
    steps:
      - uses: actions/checkout@v3
      - name: Updates Blog Outline
        uses: kayinortin/website@main
